# Dependensi TAMBAHAN untuk collector browser (TikTok/Instagram) di PC lokal.
# Pasang setelah requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements-local.txt
#   playwright install chromium
-r requirements.txt
playwright>=1.44.0
